{
    "version": "2.0.0",
    "tasks": [{
        "label": "clean-output-json",
        "command": "rm", // Could be any other shell command
        "args": ["-f","${workspaceFolder}/out-data/out.json"],
        "type": "shell"
    },
    {
        "label": "clean-output-gpkg",
        "command": "rm", // Could be any other shell command
        "args": ["-f","${workspaceFolder}/out-data/out.gpkg"],
        "type": "shell"
    },
    {
        "label": "clean-output-shp",
        "command": "rm", // Could be any other shell command
        "args": ["-f","${workspaceFolder}/out-data/out.shp"],
        "type": "shell"
    }]
}
